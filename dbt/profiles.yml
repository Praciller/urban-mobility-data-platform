urban_mobility:
  target: local
  outputs:
    local:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', env_var('DATA_DIR', './data') ~ '/processed/urban_mobility.duckdb') }}"
      schema: main
      threads: 4
