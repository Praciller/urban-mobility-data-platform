[project]
name = "urban-mobility-dagster"
version = "0.0.0"
description = "Local Dagster orchestration project for urban mobility sample pipelines."
requires-python = ">=3.12,<3.13"
dependencies = [
  "dagster>=1.12,<2",
]
