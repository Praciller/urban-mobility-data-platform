# API

FastAPI implementation is deferred to Phase 4. The API will expose health and freshness,
overview metrics, trend aggregates, zone demand, revenue summaries, anomaly results, and
bounded export endpoints without accepting arbitrary SQL.
