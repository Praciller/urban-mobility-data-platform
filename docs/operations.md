# Operations

Phase 1 supports environment setup, linting, tests, Compose validation, bootstrap image
execution, and generated-output cleanup. Full backfill, rerun, failure inspection, and reset
procedures will be added with the ingestion and orchestration phases.

Full dataset runs should set `DATA_DIR` to a directory outside OneDrive, such as
`C:/data/urban-mobility-data-platform`.
