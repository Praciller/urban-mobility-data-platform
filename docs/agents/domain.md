# Domain Docs

This repository uses a single domain context.

## Before exploring

- Read `CONTEXT.md` at the repository root.
- Read relevant records under `docs/adr/`.

Use the glossary vocabulary from `CONTEXT.md` when naming domain concepts. If a change conflicts with an ADR, surface that conflict explicitly.
